{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Z80",
            "type": "shell",
            "command": "sjasmplus",
            "args": ["${file}", "--lst=${fileDirname}/${fileBasenameNoExtension}.lst"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}